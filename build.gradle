apply plugin: 'java'

task fatJar(type: Jar) {
	manifest.attributes("Main-Class": "ru.cft.test.HelloWorld");
    baseName = 'ru.cft.test'
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}
